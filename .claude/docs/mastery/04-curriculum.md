# 4. 교육 커리큘럼

> 이 문서는 [CLAUDE-CODE-MASTERY.md](../CLAUDE-CODE-MASTERY.md)의 일부입니다.

---

## 4.0 Claude Code 설정 현황 시스템

이 시스템은 `/learn-claude-code`, `/upgrade-claude-code`에서 공통으로 사용됩니다.

### 설계 원칙

**왜 점수제를 사용하지 않는가?**

- Claude Code 기능은 계속 추가/변경됨
- 100점 만점은 "완성"을 암시하지만, 도구는 진화함
- 프로젝트마다 필요한 설정이 다름 (MCP가 필요 없는 프로젝트도 있음)

**레벨 + 체크리스트 방식의 장점**:

- 새 기능 추가 시 레벨 조건만 업데이트
- 항목별 진행 상황 명확히 파악
- "다음에 뭘 해야 하는지" 직관적

---

### 레벨 정의

| 레벨 | 이름 | 필수 조건 | 설명 |
|:----:|------|----------|------|
| 0 | 빈 프로젝트 | - | 설정 없음 |
| 1 | 기초 | CLAUDE.md ✅ | 기본 규칙 정의됨 |
| 2 | 자동화 | 레벨1 + (Commands ✅ 또는 Hooks ✅) | 반복 작업 자동화됨 |
| 3 | 전문화 | 레벨2 + (Agents ✅ 또는 Permissions ✅) | 작업 전문화됨 |
| 4 | 고급 | 레벨3 + (Skills ✅ 또는 MCP ✅) | 도메인 지식 활용 |
| 5 | 완전체 | 8개 항목 중 6개 이상 ✅ | 팀 협업 최적화 |

---

### 항목별 체크리스트

각 항목은 **존재 여부**와 **세부 요소 완성도**로 평가합니다.

#### CLAUDE.md

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | 파일 존재 | CLAUDE.md 파일이 있음 |
| ☐ | 규칙 정의 | 개발 워크플로우, 코딩 컨벤션 등 |
| ☐ | 금지사항 | ❌, 금지, don't 등 포함 |
| ☐ | 코드 예시 | ``` 블록으로 예시 포함 |
| ☐ | Progressive Disclosure | task-specific 문서 분리 (`docs/`, `agent_docs/` 등) |
| ☐ | 규칙 개수 | 150-200개 이하 권장 (모델 한계) |

**감지 패턴**: `❌`, `금지`, `하지 말`, `don't`, `forbidden`, `never`

> **Tip**: CLAUDE.md는 간결하게 유지하고, 세부 규칙은 별도 문서로 분리하세요.

#### Commands

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | 폴더 존재 | .claude/commands/ 폴더 있음 |
| ☐ | 커맨드 1개+ | 기본 제공 파일 제외하고 1개 이상 |
| ☐ | 단계별 설명 | ### 사용하여 단계 구분 |
| ☐ | frontmatter | `description`, `allowed-tools`, `argument-hint`, `model`, `hooks` |

**권장 개수**: 3개 이상 (commit, test, build 등)

**frontmatter 예시**:
```yaml
---
description: Git 커밋 생성
allowed-tools: Bash(git:*)
argument-hint: [message]
model: haiku
---
```

#### Agents

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | 폴더 존재 | .claude/agents/ 폴더 있음 |
| ☐ | 에이전트 1개+ | 최소 1개 에이전트 정의 |
| ☐ | YAML frontmatter | name, description 포함 |
| ☐ | PROACTIVELY 사용 | description에 자동 실행 조건 명시 |

**권장 개수**: 2-4개 (validator, reviewer 등)

#### Skills

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | 폴더 존재 | .claude/skills/ 폴더 있음 |
| ☐ | 스킬 1개+ | SKILL.md 포함된 폴더 1개 이상 |
| ☐ | YAML frontmatter | name, description 포함 |

**권장**: 프로젝트 도메인별 스킬 (architecture, testing 등)

#### Hooks

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | hooks 설정 | settings.local.json에 hooks 있음 |
| ☐ | PostToolUse | Write/Edit 후 포맷팅 |
| ☐ | PreToolUse | 보안 체크 (.env, secrets 보호) |

#### Permissions

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | permissions 설정 | settings에 permissions 있음 |
| ☐ | allow 설정 | 허용할 명령어 패턴 |
| ☐ | ask 설정 | 확인 후 허용할 패턴 |
| ☐ | deny 설정 | 금지할 파일/명령어 패턴 |

> **중요**: deny 규칙이 allow 규칙보다 우선순위가 높습니다.

#### MCP

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | .mcp.json 존재 | MCP 설정 파일 있음 |
| ☐ | 서버 1개+ | 최소 1개 MCP 서버 연결 |
| ☐ | 환경변수 사용 | ${VAR} 형태로 시크릿 관리 |

**중요**: MCP 서버는 프로젝트에 필요한 경우에만 설정

#### GitHub Action

| 체크 | 요소 | 설명 |
|:----:|------|------|
| ☐ | workflow 존재 | .github/workflows/에 claude 관련 파일 |
| ☐ | @claude 멘션 | PR/이슈에서 @claude로 호출 가능 |

---

### 대시보드 출력 형식

```
╔══════════════════════════════════════════════════════════════╗
║          Claude Code 설정 현황                                ║
╠══════════════════════════════════════════════════════════════╣
║ 현재 레벨: 3 (전문화)                       진행: 6/8 항목    ║
║ 다음 레벨까지: Skills 또는 MCP 중 1개 필요                    ║
╠══════════════════════════════════════════════════════════════╣
║ CLAUDE.md      ✅ 설정됨     (3/4: 규칙, 금지사항, 구조)      ║
║ Commands       ✅ 3개        (3/3: commit, test, build)      ║
║ Agents         ✅ 2개        (2/4: validator, reviewer)      ║
║ Skills         ❌ 없음       (0/?)                           ║
║ Hooks          ✅ 설정됨     (1/2: PostToolUse)              ║
║ Permissions    ✅ 설정됨     (2/3: allow, deny)              ║
║ MCP            ❌ 없음       (0/?)                           ║
║ GitHub Action  ❌ 없음       (0/1)                           ║
╠══════════════════════════════════════════════════════════════╣
║ 💡 빠른 레벨업: Skills 폴더 추가 (도메인 1개 이상)            ║
╚══════════════════════════════════════════════════════════════╝
```

**표시 규칙**:
- ✅: 항목 설정됨 (필수 요소 충족)
- ❌: 항목 미설정
- (X/Y): 세부 요소 완성도
- (?): 프로젝트마다 권장 개수가 다른 경우

---

### 빠른 레벨업 가이드

#### 레벨 0 → 1 (기초)
```bash
# CLAUDE.md 생성
1. 패키지 매니저 규칙 추가
2. 코딩 컨벤션 정의
3. 금지사항 5개 이상 추가
```

#### 레벨 1 → 2 (자동화)
```bash
# Commands 또는 Hooks 추가
1. /commit 커맨드 생성 (권장)
2. PostToolUse 포맷팅 훅 설정
```

#### 레벨 2 → 3 (전문화)
```bash
# Agents 또는 Permissions 추가
1. build-validator 에이전트 생성
2. permissions allow/deny 설정
```

#### 레벨 3 → 4 (고급)
```bash
# Skills 또는 MCP 추가
1. project-architecture 스킬 생성
2. 필요한 MCP 서버 연결 (의존성 기반)
```

#### 레벨 4 → 5 (완전체)
```bash
# 8개 항목 중 6개 이상 설정
1. GitHub Action 추가
2. 미설정 항목 보완
```

---

### 기본 제공 파일 제외 (중요!)

다음 파일/폴더는 **학습 시스템의 기본 제공 파일**이므로 평가 시 **제외**해야 합니다:

```bash
# 제외할 파일/폴더 (사용자 설정으로 카운트하지 않음)
- .claude/commands/learn-claude-code.md      # 학습 커맨드
- .claude/commands/setup-claude-code.md      # 설정 생성 커맨드
- .claude/commands/upgrade-claude-code.md    # 업그레이드 커맨드
- .claude/commands/first-claude-code.md      # 첫 프로젝트 커맨드
- .claude/docs/                              # 마스터 가이드 문서 전체
```

**예시**:
- `.claude/commands/`에 위 4개 파일만 있으면 → Commands ❌
- `.claude/commands/commit.md`가 추가로 있으면 → Commands ✅ (1/3)

---

## 4.1 레벨 1: 기초

**대상**: Claude Code 처음 사용자 또는 CLAUDE.md만 있는 프로젝트

**학습 목표**:
1. CLAUDE.md의 목적과 작성법
2. 기본 명령어 사용
3. Plan Mode 활용

**CLAUDE.md 작성 가이드**:

```markdown
# Development Workflow

## 패키지 관리
- **항상 `pnpm` 사용** (`npm`, `yarn` 금지)

## 코딩 컨벤션
- `type` 선호, `interface` 자제
- **`enum` 절대 금지** → 문자열 리터럴 유니온 사용

## 금지 사항
- ❌ console.log 사용 (logger 사용)
- ❌ any 타입
- ❌ 테스트 없이 커밋
```

**실습**:
```bash
# 1. CLAUDE.md 생성
# 2. 5개 이상의 규칙 추가
# 3. 금지사항 섹션 추가
# 4. 코드 예시 포함
```

**목표**: CLAUDE.md 완성 (4/4 체크) → 레벨 2 진입 준비

---

## 4.2 레벨 2: 자동화

**대상**: 레벨 1 완료 (CLAUDE.md ✅)

**학습 목표**:
1. 슬래시 커맨드 생성
2. PostToolUse 훅 설정
3. 권한 관리

**상세 가이드 참조**:
- Commands 템플릿: [01-settings-guide.md](01-settings-guide.md) § 1.2
- Hooks 설정: [01-settings-guide.md](01-settings-guide.md) § 1.5

**실습**:
```bash
# 1. /commit-push-pr 커맨드 생성
# 2. PostToolUse 포맷팅 훅 설정
# 3. PreToolUse 보안 훅 설정
# 4. permissions allow/deny 설정
```

**목표**: Commands ✅ + Hooks ✅ → 레벨 3 진입 준비

---

## 4.3 레벨 3: 전문화

**대상**: 레벨 2 완료 (Commands ✅ 또는 Hooks ✅)

**학습 목표**:
1. 서브 에이전트 생성
2. Skills 정의
3. MCP 통합

**상세 가이드 참조**:
- Agents 템플릿: [01-settings-guide.md](01-settings-guide.md) § 1.3
- Skills 템플릿: [01-settings-guide.md](01-settings-guide.md) § 1.4
- MCP 설정: [01-settings-guide.md](01-settings-guide.md) § 1.6

> **중요**: MCP 서버는 사용자가 명시적으로 선택해야 합니다.

**실습**:
```bash
# 1. build-validator 에이전트 생성
# 2. code-reviewer 에이전트 생성
# 3. project-architecture 스킬 생성
# 4. 필요한 MCP 서버 연결 (의존성 기반 선택)
```

**목표**: Agents ✅ + Permissions ✅ → 레벨 4 진입 준비

---

## 4.4 레벨 4: 팀 최적화

**대상**: 레벨 3 완료 (Agents ✅ 또는 Permissions ✅)

**학습 목표**:
1. GitHub Action으로 PR 자동화
2. 팀 규칙 표준화
3. 지식 축적 프로세스

**상세 가이드 참조**:
- GitHub Action 템플릿: [01-settings-guide.md](01-settings-guide.md) § 1.7

**팀 규칙 표준화**:

```markdown
## 팀 지식 축적 프로세스

1. Claude가 실수하면 → CLAUDE.md에 규칙 추가
2. PR 리뷰에서 @claude 멘션 → 규칙 자동 반영
3. 팀원 누구나 CLAUDE.md 수정 가능 (Git 관리)

## 트렌드 체크 일정
- 마지막 체크: {date}
- 다음 체크 권장: 1주일 후
- 실행: /upgrade-claude-code
```

**실습**:
```bash
# 1. GitHub Action workflow 설정
# 2. ANTHROPIC_API_KEY 시크릿 추가
# 3. 팀 CLAUDE.md 규칙 정립
# 4. 트렌드 체크 일정 추가
```

**목표**: 8개 항목 중 6개 이상 ✅ → 레벨 5 (완전체)

---

## 참고 자료

### 공식 문서
- [Claude Code Docs](https://code.claude.com/docs)
- [GitHub: claude-code-action](https://github.com/anthropics/claude-code-action)

### 글로벌 베스트 프랙티스 출처
- [HumanLayer: Writing a good CLAUDE.md](https://www.humanlayer.dev/blog/writing-a-good-claude-md)
- [Anthropic: Claude Code Best Practices](https://www.anthropic.com/engineering/claude-code-best-practices)
- [Arize: CLAUDE.md Best Practices](https://arize.com/blog/claude-md-best-practices-learned-from-optimizing-claude-code-with-prompt-learning/)
- [ClaudeLog: Hooks Guide](https://claudelog.com/mechanics/hooks/)
- [Boris Cherny's Workflow](https://howborisusesclaudecode.com/)
- [13 Claude Code Power Techniques](https://jinlow.medium.com/13-claude-code-power-techniques-insights-from-its-creator-ef4dddd3a9a1)
